# Archived PoC material

This directory contains superseded tests retained for historical reference.

The canonical execution model now lives under `poc/` and is centered on the
SDM → Execution Identity → GIE → Halos → Commit Gate → ATL boundary path.

Archived tests are not part of the canonical path and should not be extended
for new functionality.
